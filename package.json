{
  "name": "nuxeo-website-styles",
  "description": "Styles to be used on Nuxeo websites",
  "private": true,
  "version": "8.4.8",
  "main": "",
  "config": {
    "browser": "chromium-browser"
  },
  "engines": {
    "browsers": "> 0.5%, last 2 versions, Firefox ESR, not dead"
  },
  "scripts": {
    "test": "eslint --cache --fix .",
    "start": "npm run compile",
    "poststart": "npm run build:calcs",
    "predev": "npm run npm_install",
    "dev": "npm run server & nodemon --exec 'npm run compile && browser-sync reload'",
    "server": "browser-sync start --server ./static --startPath satellite/demo.html --browser=$npm_package_config_browser",
    "precompile": "bower install --allow-root",
    "compile_parallel": "npm run compile:elements & p1=$!; npm run compile:styles & p2=$!; wait \"$p1\" \"$p2\"",
    "compile": "npm run compile:elements && npm run compile:styles",
    "postcompile": "rm -rf static/satellite/latest && mkdir -p static/satellite/latest && cp -r build/es5-bundled/* static/satellite/latest && cp -r css static/satellite/latest",
    "compile:elements": "polymer build",
    "precompile:styles": "rm -rf ./tmp ./css",
    "compile:styles": "node-sass --source-map true --source-map-contents --include-path ./scss/ --include-path ./SMAKK/sass/ --include-path ./node_modules/foundation-sites/scss/ --output-style compressed --output ./tmp/ ./scss/",
    "postcompile:styles": "postcss --use autoprefixer -c postcss.json -d css/ tmp/*.css",
    "precompile:styles-expanded": "npm run precompile:styles",
    "compile:styles-expanded": "node-sass --source-map true --source-comments --include-path ./scss/ --include-path ./SMAKK/sass/ --include-path ./node_modules/foundation-sites/scss/ --output-style expanded --output ./tmp/ ./scss/",
    "postcompile:styles-expanded": "npm run postcompile:styles",
    "npm_install": "[ `npm run -s package_checksum` = `cat package.md5` ] || npm install && npm run -s package_checksum > package.md5",
    "build:calcs": "rm -rf static/roi static/tco; parcel build --no-cache --no-source-maps -d static/tco --public-url /tco src/tco/index.html; parcel build --no-cache --no-source-maps -d static/roi --public-url /roi src/roi/index.html",
    "dev:calcs": "parcel --no-cache -p 3000 src/**/index.html",
    "package_checksum": "openssl md5 package.json | grep -oE '[a-zA-Z0-9]+$'",
    "version": "npm run npm_install && npm start && cp -r static/satellite/latest static/satellite/`node -e \"console.log(require('./package.json').version);\"` && git add static/satellite && git commit -m \"Satellite release `node -e \"console.log(require('./package.json').version);\"` added\"",
    "postversion": "git push && git push --tags"
  },
  "author": "Andrew Goodricke <github@andrewgoodricke.com> (http://www.nuxeo.com)",
  "dependencies": {
    "d3": "^5.5.0",
    "foundation-sites": "6.4.2"
  },
  "devDependencies": {
    "autoprefixer": "^6.7.7",
    "bower": "^1.8.0",
    "browser-sync": "^2.18.13",
    "eslint": "^5.1.0",
    "eslint-config-prettier": "^2.9.0",
    "eslint-plugin-prettier": "^2.6.0",
    "html-minifier": "^3.4.3",
    "node-sass": "^4.9.2",
    "nodemon": "^1.12.5",
    "parcel-bundler": "^1.9.4",
    "polymer-cli": "^1.7.7",
    "postcss-cli": "^2.6.0",
    "prettier": "^1.12.1",
    "sass-lint": "^1.10.2",
    "stylelint": "^9.1.1",
    "stylelint-config-sass-guidelines": "^4.2.0",
    "stylelint-config-standard": "^18.1.0",
    "stylelint-order": "^0.8.1",
    "stylelint-scss": "^2.5.0"
  },
  "license": "MIT",
  "repository": "git@github.com:nuxeo/website-styles.git",
  "bugs": {
    "url": "git@github.com:nuxeo/website-styles.git/issues"
  }
}
